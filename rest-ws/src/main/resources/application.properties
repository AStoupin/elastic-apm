server.port=8888
spring.application.name=rest1

logging.file=${spring.application.name}.log 
logging.level.root=info
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} %-5level [tc.id=%X{trace.id} tx.id=%X{transaction.id}] %logger{36} - %msg%n
logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss} %-5level [tc.id=%X{trace.id} tx.id=%X{transaction.id}] %logger{36} - %msg%n
